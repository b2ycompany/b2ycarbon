function showScreen(screenId) { document.querySelectorAll('div[id$="-screen"]').forEach(screen => { screen.style.display = 'none'; }); document.getElementById(screenId + '-screen').style.display = 'block'; } function handleRegister(event) { event.preventDefault(); // Captura os dados do formulário const fullName = document.getElementById("full-name").value; const email = document.getElementById("email").value; const vehicle = document.getElementById("vehicle").value; const licensePlate = document.getElementById("license-plate").value; // Atualiza os dados na tela de dashboard document.getElementById("user-name").innerText = `Nome: ${fullName}`; document.getElementById("user-email").innerText = `Email: ${email}`; // Mostra a tela de dashboard showScreen("dashboard"); // Inicializa o mapa setTimeout(initializeMap, 100); // Timeout para evitar erro de carregamento } function initializeMap() { const map = L.map("map").setView([-23.5505, -46.6333], 13); L.tileLayer("https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png", { attribution: "© OpenStreetMap contributors", }).addTo(map); // Adiciona um marcador no mapa L.marker([-23.5505, -46.6333]) .addTo(map) .bindPopup("Você está aqui!") .openPopup(); } document.addEventListener("DOMContentLoaded", () => { showScreen("welcome"); });
