function showScreen(screenId) { document.querySelectorAll('div[id$="-screen"]').forEach(screen => { screen.style.display = 'none'; }); document.getElementById(screenId + '-screen').style.display = 'block'; } function handleRegister(event) { event.preventDefault(); showScreen('dashboard'); } // Inicializa o mapa document.addEventListener('DOMContentLoaded', () => { showScreen('welcome'); const map = L.map('map').setView([-23.5505, -46.6333], 13); L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png', { attribution: '© OpenStreetMap contributors', }).addTo(map); });
